# OpsNest Desktop 2.13.6

- An authenticated owner or administrator can now open Desktop even when the
  shared workspace has no snapshot yet. This supports the first company setup
  without lowering member permissions or bypassing central authentication.
- Other team roles remain blocked from an empty workspace until an owner or
  administrator publishes the first protected shared revision.
